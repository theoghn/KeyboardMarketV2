spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kbdmarket
    username: theo
    password: meow-meow
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet